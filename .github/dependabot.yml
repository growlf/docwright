# DocWright Dependabot configuration
#
# Version updates are DISABLED. Dependency bumps are handled deliberately
# via DocWright proposals and plans — not on an automated schedule.
#
# Security updates (CVE patches) remain enabled via repo settings and will
# auto-create PRs only when a known vulnerability is detected.

version: 2
updates: []
