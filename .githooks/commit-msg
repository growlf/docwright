#!/bin/bash
# Thin shim — the canonical hook source is scripts/commit-msg.sh (#144).
# Do NOT add logic here; edit the canonical source. The drift-guard test
# (test/hooks/test-hook-source-unification.sh) fails if this grows.
exec "$(git rev-parse --show-toplevel)/scripts/commit-msg.sh" "$@"
