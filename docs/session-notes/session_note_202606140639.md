# Session Note: 2026-06-14 — Phoenix Arc GPU fix + local AI reliability

**Date:** 2026-06-14
**Author:** NetYeti (growlfd@gmail.com) @ phoenix
**Focus:** Fix GPU acceleration on Phoenix (Meteor Lake Arc), establish reliable local model path

## Problem

The local Ollama on Phoenix (Intel Arc Graphics, Meteor Lake-P) runs 100% CPU.
LLama 3.1 8B can't handle tool calls — outputs raw JSON instead of executing them.
This makes the `profile-full-local/auto` path unreliable when cloud tokens aren't available.

## Architecture Context

- **Cluster-llm** (10.10.0.201, RTX 3090 Ti): runs full ai-stack Docker stack (ollama, litellm, olla:40114, smart-router:40115, retriever)
- **Phoenix** (this machine, Intel Arc): remote herd node only, native Ollama via BigDL (broken GPU)
- **Smart router** is healthy and routing correctly — test queries just aren't reaching it

## Plan

### Phase 1: Fix GPU acceleration on Phoenix
1. Stop broken BigDL-embedded Ollama (orphaned process, 100% CPU)
2. Fix systemd `ollama.service` to:
   - Point WorkingDirectory to `/home/netyeti/Projects/ai-stack`
   - Run `docker compose -f docker-compose.yml -f docker-compose.arc.yml up -d ollama`
   - Only start the ollama container (not the full stack — cluster-llm handles olla/router/litellm)
3. Start and verify GPU acceleration via `ollama ps` (should show GPU %, not CPU)
4. Test with qwen2.5-coder:14b (9GB, T2 tool-capable, already cached)

### Phase 2: Test model routing
1. Verify `profile-full-local/auto` resolves through smart router to a tool-capable model
2. Confirm tool calls work end-to-end

### Phase 3: Document known-good state
1. Update ai-stack docs with standalone-node ollama+Docker+Arc pattern
2. Update intel_nuc_skullcanyon_ollama_with_gpu with Meteor Lake Arc specifics + xe driver notes

## Key Details

- Kernel driver: `xe` (not `i915` — this is the modern Intel Xe driver for Meteor Lake+)
- GPU: `Intel Corporation Meteor Lake-P [Intel Arc Graphics] (rev 08)`
- Arc overlay image: `intelanalytics/ipex-llm-inference-cpp-xpu:latest`
- daocloud mirror: `intelanalytics/ipex-llm-inference-cpp-xpu:latest` (for Great Firewall scenarios)
- GPU passthrough: `/dev/dri:/dev/dri` (whole directory, avoids card0/card1 drift)
