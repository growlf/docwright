# Session Note: 2026-06-17 — Policy Atom Framework + Completion Gate + AI Taxonomy

**Date:** 2026-06-17
**Author:** NetYeti @ phoenix
**Focus:** Complete policy atom framework (Steps 1–5), AI task category taxonomy (Steps 0–2), completion gate enforcement bug, executor UX

## Summary

Marathon session completing two major plans and filing/fixing a critical governance bug. The policy atom framework plan (Steps 1–5) is now complete and in `plans/completed/`. The AI task category taxonomy plan progressed through Step 0 (atomic plan generator) and Steps 1–2 (AiCategory extended to 6 values, categories.yaml registry). A critical completion gate enforcement bug was discovered, fixed, and its own plan completed in the same session. The executor panel heartbeat fix prevents future human confusion during BigPickle runs.

## Decisions Made

- `AiCategory` extended from 4 to 6 values: added `coding` and `agentic` — justified by DocWright task-type audit
- Absent `tests_human_reviewed` field = blocks completion (same as false) — intentional governance
- `callOpenCode` polling approach confirmed: POST message, discard SSE body, poll GET for completion
- `automated: full` plans get Start (Auto) button; `guided` plans just set in-progress — clarified
- Completion gate enforcement: both `## Phase Gate` (legacy) and `### Gate Criteria` (current) now recognized

## Plans Worked On

| Plan | Status |
|------|--------|
| Policy Atom Framework — Generic Tiered Policy Engine | ✅ Completed → plans/completed/ |
| Plan Completion Gate Enforcement Bug | ✅ Completed → plans/completed/ |
| AI Task Category Taxonomy | 🔄 In Progress (Steps 0–2 done, Steps 3–5 pending) |
| adopt-vault plan | Already completed, BigPickle correctly moved to plans/completed/ |

## Key Deliverables This Session (53 commits)

- `policy-atoms-core`: Steps 1–5 complete — schema, scope, index-builder, router, resolver, sync-checker, 10 atoms, coexistence shim, manager/project separation, hook stubs
- `AiCategory` extended: `coding` (→ qwen2.5-coder:14b) and `agentic` (→ mistral-small3.2:24b) added
- `src/policy-atoms-core/categories.yaml`: formal display metadata registry for all 6 categories
- `docs/policy-atom-model-routing.md`: routing tables for `coding` and `agentic` added
- `approve-proposal/plan-generator.ts`: atomic plan generation (5 OpenCode calls) replaces naive template parser
- `src/executor/session.ts`: heartbeat events every 5s during BigPickle session — no more silent Execute panel
- `src/mcp/lib/steps.ts`: `checkCompletionGate` now recognizes `### Gate Criteria`; `tests_human_reviewed` gates completion
- `src/webui/src/lib/PropertiesPane.svelte`: Complete button shows "Complete (N blockers)" when gate criteria unmet
- Deferred proposals filed: judgment-atom mode interaction, frontmatter-validate assigned_to strictness, executor panel feedback
- `proposals/executor-panel-live-feedback.md`: documents Fixes 2+3 still pending (step name + token count)

## Uncommitted / Known Gaps

- `run-tests` endpoint doesn't include `test:atoms` or `atoms:isolation` — needs fix
- MCP Python test file `test/mcp/test-plan-tools.py` missing — pre-existing, needs resolution
- Step 7a (tagging OpenCode skills distributable) was done but Web UI stripped the plan entry — recorded in Document History
- Cascade STEAM vault backup at `~/Projects/bms-ai-cluster.backup-20260616-185155` — can be cleaned up

## Commits This Session

53 commits from `26ec582` (Step 1 — policy-atoms-core spike) to `171af91` (stale plan staging). All pushed to main.

## Next Session Should Start With

- File proposal: `run-tests` endpoint missing `test:atoms` — one-line fix in `src/webui/src/routes/api/lifecycle/run-tests/+server.ts`
- Resolve missing `test/mcp/test-plan-tools.py` (restore or remove reference)
- Consider Steps 3–4 of AI Task Category Taxonomy (ai-stack capability registry is ai-stack workstream; Step 4 is UI sprint)
- Gate review the completed plans is done — no backlog there
