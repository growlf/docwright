# Session Note: 2026-06-29 — Chat Write-back Debugging

**Date:** 2026-06-29
**Author:** NetYeti @ phoenix
**Focus:** Chat write-back — AI reads file and uses tools but edit fails

## RESUME HERE NEXT SESSION

**The specific task in progress:** Make the DocWright chat AI actually write changes to the open document when the user gives a clear instruction like "please remove the redundancies in the Testing Plan."

### Current state (verified)

The inline context injection IS working:
- ✅ AI receives the file path with the user's first message
- ✅ AI uses the `read` tool to read the file
- ✅ AI identifies the correct redundancy (two duplicate step lists in Testing Plan)
- ✅ AI calls the `edit` tool with the right intent
- ❌ The `edit` tool's **exact string matching** fails silently on 196-line files

The `edit` tool uses `str_replace` which requires the `oldString` to match the file byte-for-byte. On a large file, the AI's reconstructed string often differs slightly, causing a silent no-op.

### The fix needed (one change)

Update the inline context instruction to tell the AI to use the `write` tool (full file rewrite) rather than `edit` (string replace) for section-level changes:

**In `ChatPanel.svelte`, find the `docCtx` string (around line 676) and change:**
```
Rules: read the file first, make changes directly on clear instructions, write the file using edit/write tools...
```
**To:**
```
Rules: read the file first, make changes directly on clear instructions. For removing or replacing sections, use the write tool to rewrite the whole file — do NOT use edit/str_replace which fails on large files. Never change approved:/status:completed/gate_status: fields.
```

Then test by sending "please remove the redundancies in the Testing Plan" and verifying the file changes.

### Branch: `feat/chat-doc-context`

All changes are on this branch. Not merged yet — pending this verification.

## What also needs doing this session (after the write-back fix)

1. **Verify write-back works** with the `write` tool instruction
2. **Run `npm run test:chat-sessions`** against live server
3. **Run `npm run test:ai-gateway`** against live server  
4. **Close `chat-architecture-document-scoped-sessions` plan** (all 6 steps done)
5. **Close `unify-ai-via-opencode` plan** (all 8 steps done)
6. **Open PRs** for both feature branches and merge

## OAuth token note

`ANTHROPIC_AUTH_TOKEN` in `.env` was reverted back to `ANTHROPIC_API_KEY` (Cascade STEAM key, no credits). Claude via OpenCode requires either API credits OR a different auth approach. Use big-pickle (local model) for testing write-back.

## Session note

```bash
# Start DocWright
scripts/DocWright

# The FIRST thing to do:
# Fix the write tool instruction in ChatPanel.svelte docCtx string
# Then: open plans/multiuser-auth-concurrent-sessions.md
# Hit ↺ in chat, say "please remove the redundancies in the Testing Plan"
# Verify the file changes
```
