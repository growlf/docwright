# Session Note: 2026-06-22 — Asset Discovery & SSH Config Rules

**Date:** 2026-06-22
**Author:** NetYeti @ phoenix
**Focus:** Asset discovery toolset buildout

## Summary

Built a unified asset discovery toolset across DocWright and bms-ai-cluster:
layered validation framework, unified Ansible playbook, sync script with drift
detection, web UI notification system, and external plugin prototype
(asset-management profile). Ran physical discovery on 4 hosts (pve1, pve2,
crash, frank). Added 2 new standing rules. Recovered SSH access to frank and
crash by adding missing SSH config aliases.

## Decisions Made

- Single unified Ansible playbook `gather-device-inventory.yml` replaces separate
  tools for discovery purposes; older `gather-network-facts.yml` kept for AI cluster
- Sync script `sync-device-from-discovery.py` is companion to `sync-device-inventory.py`
  — one writes from discovery data, other generates .md views from resulting YAMLs
- Notification system uses persistent store (not ephemeral toasts) so drift alerts
  survive page navigation
- External plugin architecture prototyped through `asset-management` profile:
  DocWright defines use-case + visualization, bms-ai-cluster implements tooling

## Rules Added

- `ssh-config-only.md` — all SSH via ~/.ssh/config aliases, never raw IPs
- `password-manager-first.md` — update BW before continuing after any credential create/modify

## Plans Worked On

No plans were created or completed this session — this was discovery tooling work
that followed the "offer choices, don't guess" principle under the existing plan.

## Commits This Session

```
2226822 fix: track new OpenCode rules (password-manager-first, ssh-config-only)
```

## Uncommitted Changes

Modified: .opencode/rules/one-off-formalization.md, AGENTS.md, +layout.svelte
Untracked: docs/SOPs/asset-discovery.md, .opencode/skills/docwright-discovery/,
src/profiles/asset-management/, src/webui/src/lib/notifications.ts

## Architecture State

Working toward a full BMS scan. SSH aliases all fixed. Router (10.10.0.1)
password in Bitwarden is stale — access recovery needed from BMS (same L2).

## Next Session Should Start With

The BMS router (10.10.0.1) password in Bitwarden is stale — needs recovery.
See `docs/session-notes/BMS_DISCOVERY_DATA.md` for full context and first
actions. Main priority: recover router access, dump ARP, run the Ansible
playbook, sync discovery data, resolve orphan iDRACs.
