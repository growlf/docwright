# Session Note: 2026-06-28/29 — Unify AI via OpenCode, Chat Context + Write-back

**Date:** 2026-06-28
**Author:** NetYeti @ phoenix
**Focus:** Unify all AI calls through OpenCode; fix chat context and write-back

## Summary

Completed the `unify-ai-via-opencode` plan (8 steps): all three OLLA routes (plan-review,
apply-review, synthesize) now route through OpenCode's session API via a new
`opencodeComplete()` helper. OLLA_* env vars removed; ANTHROPIC_API_KEY wired into
OpenCode's environment via the launcher script. `/api/config` created. Approved and
implemented two high-priority chat bugs: (1) chat had no document context — now injects
the active document into the first message of a new session; (2) chat was advisory-only —
now injects a DocWright system prompt on session creation telling the AI its role, vault
root, and governance constraints. Steps 2–3 of both chat plans need live-server
verification next session.

## Decisions Made

- All AI calls go through OpenCode — single model picker, single credential store
- `opencodeComplete()` uses OpenCode's session API (not OpenAI-compat endpoint — OC doesn't expose one)
- System prompt injected as `role: 'system'` after session creation — falls through as user message if OC ignores role
- Context injected only on first message in a new session (not retroactively)
- `OPENCODE_DEFAULT_MODEL=anthropic/claude-haiku-4-5-20251001` pins review routes; chat follows OC picker

## Plans Worked On

| Plan | Status |
|------|--------|
| unify-ai-via-opencode | ✅ All 8 steps done — needs smoke test on live server |
| chat-active-document-context | Steps 1–2 done, Step 3 needs live server |
| chat-document-write-back | Step 1 done, Steps 2–3 need live server |

## Commits This Session

```
e2c001e feat: chat active document context + write-back system prompt
b295968 feat: write steps for chat-context + write-back plans; transition proposals
6d7cb59 feat: approve proposals — chat context + write-back HUMAN-APPROVED:NetYeti
b9d0c29 feat: two proposals — chat active document context + write-back (high priority)
4d23927 feat: /api/config endpoint + ai-gateway smoke tests
cbdb6e3 chore: remove OLLA_* vars, document OpenCode-only AI config
d77bcd0 feat: rewrite plan-review, apply-review, synthesize to use opencodeComplete()
2f0f1c9 feat: opencodeComplete() — single-turn AI helper via OpenCode session API
76c268a feat: launcher sources webui .env before opencode serve
09f7e7f feat: write implementation steps for unify-ai-via-opencode plan
```

## Uncommitted Changes

- `plans/chat-active-document-context.md` — step status updates (will be committed below)
- `plans/chat-document-write-back.md` — step status updates

## Architecture State

- All AI routes unified through OpenCode (`OPENCODE_URL=http://localhost:4096`)
- `ANTHROPIC_API_KEY` in `src/webui/.env` (gitignored); launcher sources it for OpenCode
- `OPENCODE_DEFAULT_MODEL=anthropic/claude-haiku-4-5-20251001` pins review/synthesize model
- Chat panel: `currentDocPath` + `currentDocContent` props from `$currentDoc` store
- Active branches: `feat/unify-ai-via-opencode`, `feat/chat-doc-context`

## Next Session Should Start With

```bash
# 1. Restart DocWright (picks up ANTHROPIC_API_KEY in OpenCode)
#    scripts/DocWright --vault /path/to/vault
# 2. Verify Claude models appear in OpenCode model picker
# 3. Run smoke tests: npm run test:ai-gateway
# 4. Test chat context: open a plan, ask about it — AI should reference specific content
# 5. Test write-back: "fix the duplication in Testing Plan" — AI should write the file
# 6. Close out unify-ai-via-opencode and chat plans after verification
# 7. Next big work: multiuser-auth (critical) or governance-engine-view-container (high)
```
